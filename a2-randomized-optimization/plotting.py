import numpy as np
import pandas as pd
import matplotlib.pyplot as plt


def plot_fitness_curves(df, title, x_lim=None, y_lim=None):
    """
    Parameters
    ----------
    df : dataframe that contains fitness curves for each run
        each row is a separate iteration, each column a separate run

    title : string
        Title for the chart.

    """
    pass


def plot_time_complexity_curves(df, title):
    pass
